<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EscapeTheJail"
        tools:targetApi="31">
        <activity
            android:name=".core_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".social_activity" />
        <activity android:name=".levels_activity" />
        <activity android:name=".leader_boards_activity" />
        <activity android:name=".settings_activity" />
        <activity android:name=".add_friend_activity" />
        <activity android:name=".high_score_activity" />
        <activity android:name=".friend_list_activity" />
        <activity android:name=".log_in_activity" />
        <activity android:name=".register_activity" />
    </application>

</manifest>